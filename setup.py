from setuptools import setup

setup(name="python-to-r"
      version="BUILD"
      description="A simple wrapper app which talks to R functions using rpy2"
      packages=['python-to-r']
      )
    
